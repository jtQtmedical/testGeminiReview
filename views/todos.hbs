<div class="row">
    <div class="col-md-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h2">
                <i class="fas fa-tasks me-2 text-primary"></i>
                我的待辦事項
            </h1>
            <a href="/todos/new" class="btn btn-primary">
                <i class="fas fa-plus me-1"></i>
                新增待辦事項
            </a>
        </div>

        {{#if todos.length}}
            <div class="row">
                {{#each todos}}
                <div class="col-md-6 col-lg-4 mb-3">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title">
                                {{#if completed}}
                                    <i class="fas fa-check-circle text-success me-2"></i>
                                    <span class="text-decoration-line-through text-muted">{{title}}</span>
                                {{else}}
                                    <i class="fas fa-circle text-warning me-2"></i>
                                    {{title}}
                                {{/if}}
                            </h5>
                            {{#if description}}
                                <p class="card-text {{#if completed}}text-muted{{/if}}">{{description}}</p>
                            {{/if}}
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">
                                    <i class="fas fa-calendar me-1"></i>
                                    {{formatDate createdAt}}
                                </small>
                                <div class="btn-group" role="group">
                                    <a href="/todos/{{id}}/edit" class="btn btn-sm btn-outline-primary">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <button type="button" class="btn btn-sm btn-outline-danger" onclick="deleteTodo({{id}})">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                    {{#unless completed}}
                                        <button type="button" class="btn btn-sm btn-outline-success" onclick="completeTodo({{id}})">
                                            <i class="fas fa-check"></i>
                                        </button>
                                    {{/unless}}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        {{else}}
            <div class="text-center py-5">
                <i class="fas fa-clipboard-list fa-5x text-muted mb-3"></i>
                <h3 class="text-muted">目前沒有待辦事項</h3>
                <p class="text-muted">點擊上方的「新增待辦事項」按鈕來建立您的第一個待辦事項！</p>
                <a href="/todos/new" class="btn btn-primary btn-lg">
                    <i class="fas fa-plus me-2"></i>
                    新增待辦事項
                </a>
            </div>
        {{/if}}
    </div>
</div> 